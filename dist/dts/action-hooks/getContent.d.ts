type ContentArgs = {
    path: string;
    version?: string;
    page?: number;
    fullObjects?: boolean;
};
export declare const useGetContent: ({ path, version, page, fullObjects, }: ContentArgs) => any[];
export {};
